<h1>New Company</h1>

<%= form_for @company do |c| %>
   <div>
     <%=  c.label :name %>
     <%=  c.text_field :name %>
   </div>
    <div>
      <%= c.submit 'Create Company' %>
    </div>
<% end %>

<% content_for :aside do %>
    <%= render 'sidebar' %>
<% end %>
